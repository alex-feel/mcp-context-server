# Docker Compose for MCP Context Server with External PostgreSQL + Ollama Embeddings
# PostgreSQL must be deployed separately (corporate server, Supabase, etc.)
#
# Setup:
#   1. Copy .env-postgresql-external.ollama.example to .env in this directory
#   2. Fill in your PostgreSQL connection details in .env
#   3. Run: docker compose -f deploy/docker/docker-compose.postgresql-external.ollama.yml up -d

name: context-server

services:
  mcp-context-server:
    image: mcp-context-server
    build:
      context: ../..
      dockerfile: Dockerfile
    pull_policy: build
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      - MCP_TRANSPORT=http
      - FASTMCP_HOST=0.0.0.0
      - FASTMCP_PORT=8000
      # Disable Rich log formatting and ASCII banner for clean single-line container logs
      - FASTMCP_ENABLE_RICH_LOGGING=false
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      # PostgreSQL backend (connection details from .env file)
      - STORAGE_BACKEND=postgresql
      # Semantic search configuration
      - ENABLE_SEMANTIC_SEARCH=true
      # Embedding provider selection (default: ollama)
      - EMBEDDING_PROVIDER=${EMBEDDING_PROVIDER:-ollama}
      - OLLAMA_HOST=http://ollama:11434
      - EMBEDDING_MODEL=${EMBEDDING_MODEL:-qwen3-embedding:0.6b}
      - EMBEDDING_DIM=${EMBEDDING_DIM:-1024}
      # Full-text and hybrid search
      - ENABLE_FTS=true
      - ENABLE_HYBRID_SEARCH=true
      # Summary generation
      - SUMMARY_MODEL=${SUMMARY_MODEL:-qwen3:0.6b}
      - SUMMARY_PROVIDER=${SUMMARY_PROVIDER:-ollama}
      # FlashRank reranking model cache (persistent across restarts)
      - RERANKING_CACHE_DIR=/cache/flashrank
      # LangSmith tracing (optional, disabled by default)
      # - LANGSMITH_TRACING=false
      # - LANGSMITH_API_KEY=
      # - LANGSMITH_PROJECT=mcp-context-server
    volumes:
      - flashrank-cache:/cache/flashrank
    depends_on:
      ollama:
        condition: service_healthy
    restart: on-failure:5

  ollama:
    build:
      context: ../..
      dockerfile: deploy/docker/ollama/Dockerfile
    pull_policy: build
    environment:
      - OLLAMA_HOST=0.0.0.0
      - OLLAMA_KEEP_ALIVE=-1
      - EMBEDDING_MODEL=${EMBEDDING_MODEL:-qwen3-embedding:0.6b}
      - SUMMARY_MODEL=${SUMMARY_MODEL:-qwen3:0.6b}
    volumes:
      - ollama-data:/root/.ollama
    # Uncomment to enable NVIDIA GPU acceleration (requires NVIDIA Container Toolkit)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]
    healthcheck:
      test: ["CMD-SHELL", "ollama show \"$${EMBEDDING_MODEL}\" >/dev/null 2>&1 && ollama show \"$${SUMMARY_MODEL}\" >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 30s
      retries: 30
      start_period: 120s
    restart: unless-stopped

volumes:
  ollama-data:
    name: ollama-models
  flashrank-cache:
    name: flashrank-model-cache
