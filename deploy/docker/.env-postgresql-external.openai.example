# =============================================================================
# External PostgreSQL + OpenAI Embeddings Configuration (REQUIRED)
# =============================================================================
# For use with: docker-compose.postgresql-external.openai.yml
#
# Copy to .env and configure your OpenAI API key and PostgreSQL connection.
# For detailed PostgreSQL setup, see: docs/deployment/docker.md
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: OpenAI API Key
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-your-openai-api-key

# -----------------------------------------------------------------------------
# REQUIRED: PostgreSQL Connection
# -----------------------------------------------------------------------------
POSTGRESQL_HOST=localhost
POSTGRESQL_PORT=5432
POSTGRESQL_USER=postgres
POSTGRESQL_PASSWORD='your-password'
POSTGRESQL_DATABASE=mcp_context
POSTGRESQL_SSL_MODE=prefer

# Alternative: Connection string (overrides above if set)
# POSTGRESQL_CONNECTION_STRING=postgresql://user:pass@host:5432/db

# -----------------------------------------------------------------------------
# Optional: LangSmith Tracing
# -----------------------------------------------------------------------------
# LANGSMITH_TRACING=true
# LANGSMITH_API_KEY=lsv2_your-langsmith-api-key
# LANGSMITH_PROJECT=mcp-context-server

# -----------------------------------------------------------------------------
# Optional: FastMCP Logging (recommended for Docker/cloud deployments)
# -----------------------------------------------------------------------------
# Disable Rich log formatting to prevent multi-line wrapping in container logs
# FASTMCP_ENABLE_RICH_LOGGING=false

# -----------------------------------------------------------------------------
# Optional: Logging Configuration
# -----------------------------------------------------------------------------
# LOG_LEVEL=DEBUG

# -----------------------------------------------------------------------------
# Optional: Model and Provider Configuration
# -----------------------------------------------------------------------------
# Uncomment to override the default model or provider for this deployment.
# Defaults shown below match the compose file defaults.
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_DIM=1536
# EMBEDDING_PROVIDER=openai
# SUMMARY_MODEL=gpt-5-mini
# SUMMARY_PROVIDER=openai

# -----------------------------------------------------------------------------
# Optional: PostgreSQL Connection Pool Settings
# -----------------------------------------------------------------------------
# POSTGRESQL_POOL_MIN=2
# POSTGRESQL_POOL_MAX=20
